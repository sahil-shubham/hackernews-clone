{
    # Global options block
    # acme_dns # If you use a DNS provider for ACME challenges (e.g. Cloudflare, Route53)
    # For example, for Cloudflare: acme_dns cloudflare {env.CLOUDFLARE_API_TOKEN}
    # Ensure you set CLOUDFLARE_API_TOKEN as an environment variable in docker-compose.yml for Caddy if you use this.

    # Email for ACME (SSL certificate) registration and recovery
    # It's good practice to set this.
    email spam@sahil-shubham.in
}

# Replace your-domain.com with your actual domain
spc.sahil-shubham.in {
    # Enable compression
    encode zstd gzip

    # Log requests
    log {
        output stdout
        format console
    }

    # Reverse proxy requests to your Next.js app service
    # The name 'app' comes from your docker-compose.yml service name
    reverse_proxy app:3000
}

# Optional: Redirect www to non-www (or vice-versa)
www.spc.sahil-shubham.in {
    redir https://spc.sahil-shubham.in{uri} permanent
}